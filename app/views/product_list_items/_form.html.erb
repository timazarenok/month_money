<%= form_with(model: product_list_item, local: true) do |form| %>
  <% if product_list_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_list_item.errors.count, "error") %> prohibited this product_list_item from being saved:</h2>

      <ul>
      <% product_list_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_category_id %>
    <%= form.text_field :product_category_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
