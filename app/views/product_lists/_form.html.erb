<%= form_with(model: product_list, local: true) do |form| %>
  <% if product_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_list.errors.count, "error") %> prohibited this product_list from being saved:</h2>

      <ul>
      <% product_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :month_id %>
    <%= form.text_field :month_id %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
